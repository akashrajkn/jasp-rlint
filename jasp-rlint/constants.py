
MAX_LINE_LENGTH = 100

ERROR_MESSAGES = {
    "gt_max_line_length": "line length is greater than maximum allowed",
    "no_space_after_if": "there should be a space between 'if' and parenthesis",
    "indentation_error": "indentation is not as expected"
}
